%% Imagem em tons de cinza 1
%%img = imread('pout.tif');
%%contrast_stretching_plot(img, 80, 160, 5, 255);
%%equalizacao_histograma_plot(img);

%% Imagem em tons de cinza 2
%%img = imread('cameraman.tif');
%%contrast_stretching_plot(img, 100, 200, 30, 250);
%%equalizacao_histograma_plot(img);

%% Imagem colorida 1
%%img = imread('coloredChips.png');

%%img_red_channel = img(:,:,1);
%%img_green_channel = img(:,:,2);
%%img_blue_channel = img(:,:,3);

%%contrast_stretching_plot(img_red_channel, 120, 240, 10, 200);
%%contrast_stretching_plot(img_green_channel, 120, 240, 50, 210);
%%contrast_stretching_plot(img_blue_channel, 100, 220, 50, 200);

%% Imagem colorida 2
%%img = imread('./colours-colors.jpg');

%%img_red_channel = img(:,:,1);
%%img_green_channel = img(:,:,2);
%%img_blue_channel = img(:,:,3);

%%contrast_stretching_plot(img_red_channel, 10, 240, 40, 220);
%%contrast_stretching_plot(img_green_channel, 30, 160, 50, 210);
%%contrast_stretching_plot(img_blue_channel, 10, 240, 25, 230);